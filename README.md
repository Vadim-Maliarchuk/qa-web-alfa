@cypress/xpath устарел и больше не поддерживается

В тестах используются CSS селекторы из-за прекращения поддержки @cypress/xpath.

Запуск тестов

установить зависимости npm i

для запуска GUI cypress npm run cy:open
